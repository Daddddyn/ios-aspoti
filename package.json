{
  "name": "aspoti",
  "version": "1.0.0",
  "description": "Aspotï — free music, no limits",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "sync": "npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
